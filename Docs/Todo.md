# TODO

## High Priority
- Setup `PlayerController.cs` and `PlayerStates`.
- Implement `PlayerInputHandler.cs` using Unity's new Input System.
- Set up Unity Netcode for GameObjects and ensure `NetworkGameManager` functions properly.

## Medium Priority
- Define `ItemData` ScriptableObjects.
- Set up basic `ReactorController.cs` for ship systems.

## Low Priority
- Implement `MissionResultUI.cs`.
- Add sound effects for interacting and repairing.
